int test(a, b, c, d: int) {
    x, y, z: int;
    x = a + b;
    y = x * c;
    z = y / d;
    return z;
}

void main() {
    a, b, c, d, res: int;
    read(a);
    read(b);
    read(c);
    read(d);
    res = test(a, b, c, d);
    write(res);
}
