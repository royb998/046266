float test(n: int, ...) {
    if (n > 0) then {
        x = (float)n + va_arg(float, n - 1);
    }
    return x;
}

void main() {
    a, b, c, res: float;
    read(a);
    read(b);
    read(c);
    res = test(3, a, b, c);
    write(res);
}
