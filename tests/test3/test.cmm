float test(a, b, c: float) {
    x, y: float;
    x = (a + b) * 3.5;
    y = (x + c) / 3.2;
    return y * 10;
}

void main() {
    a, b, c, res: float;
    read(a);
    read(b);
    read(c);
    res = test(a, b, c);
    write(res);
}
